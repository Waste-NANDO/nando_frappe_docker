# Dev stack — custom apps, port 3003, Compose project "erpnext" (preserves existing volumes).
ERPNEXT_VERSION=v16.5.0
# frappe/build and frappe/base Docker tag (major line only — not a git pin)
FRAPPE_BRANCH=version-16
# Frappe framework git tag for bench init (required; do not use version-16)
FRAPPE_VERSION=v16.20.0
DB_PASSWORD=CHANGE_ME_DB_PASSWORD

COMPOSE_PROJECT_NAME=erpnext
COMPOSE_FILE_OUTPUT=nando-deployment/erpnext-dev.yaml
TRAEFIK_ROUTER_PREFIX=erpnext
TRAEFIK_HOST_RULE='Host(`apps.internal.nandoai.com`)'

FRAPPE_SITE_NAME_HEADER=apps.internal.nandoai.com
FRAPPE_HOST_NAME=https://apps.internal.nandoai.com:3003
HTTPS_PUBLISH_PORT=3003

GCS_BUCKET=nando-it-backups
APP_NAME=nando-erp-dev

INCLUDE_CUSTOM_APP=yes
# Comma-separated app keys (directory names under nando-deployment/custom-apps/)
CUSTOM_APP_KEYS=nando_crm,nando_fulfillment
# Apps to install on the site (subset or same as CUSTOM_APP_KEYS)
SITE_INSTALL_APPS=nando_crm,nando_fulfillment

NANDO_CRM_REPO=https://github.com/Waste-NANDO/nando-erp-crm.git
NANDO_CRM_BRANCH=dev

NANDO_FULFILLMENT_REPO=https://github.com/Waste-NANDO/nando-erpnext-module.git
NANDO_FULFILLMENT_BRANCH=dev

INCLUDE_HRMS=yes
# Pin to the same release tag as ERPNEXT_VERSION (version-16 branch tracks latest HRMS and can break)
HRMS_BRANCH=v16.5.0

CUSTOM_IMAGE=nando-erpnext-custom
CUSTOM_TAG=v16.5.0-custom-crm-frappe-16.20
PULL_POLICY=never

# Compile JS/CSS during docker build. HRMS PWA/roster skipped by default (BUILD_HRMS_FULL=0).
BUILD_ASSETS_IN_IMAGE=yes
BENCH_BUILD_NODE_MEMORY_MB=6144
BUILD_HRMS_FULL=0
